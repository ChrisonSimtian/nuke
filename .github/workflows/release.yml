# Hand-written (not auto-generated) — see Build.CI.GitHubActions.cs for the
# rationale (lets us bridge GitHub's NUGET_API_KEY secret to Fallout's
# NuGetApiKey parameter, which would otherwise have to share a name).

name: release

# RELEASE-BRANCH MODEL (milestone #13, RFC #267): this workflow lives on the
# release/v10 maintenance line and publishes the 10.3.x channel to nuget.org.
# It fires on merge to release/v10 — releases here are rare (maintenance + the
# clean 10.3.x marker), so the per-merge auto-publish that was disabled on `main`
# (Dependabot noise) is fine on this branch. The first publish from this branch
# is the clean 10.3.49 marker — the inaugural test of release-from-release/vN.
on:
  push:
    branches:
      - release/v10
    paths-ignore:
      - 'docs/**'
      - '.assets/**'
      - '**/*.md'
  workflow_dispatch: {}

permissions:
  contents: write     # for ICreateGitHubRelease (tag + GitHub release)
  packages: write     # for the Nuke.* transition-shim push to GH Packages (#47)

jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    # Deploys to the `nuget-org` GitHub Environment (mirrors main, #273/#280):
    #   - resolves secrets.NUGET_API_KEY from the env-scoped secret (the
    #     repo-scoped one is being retired);
    #   - fires the environment's required-reviewer approval before publish.
    # NOTE: the env may carry a deployment-branch policy — if release/v10.3
    # isn't in its allowed list, this job is blocked until it's added.
    environment:
      name: nuget-org
      url: https://www.nuget.org/profiles/Fallout
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0   # Nerdbank.GitVersioning needs full history
          submodules: recursive   # vendor/vs-solutionpersistence
      - name: 'Cache: .fallout/temp, ~/.nuget/packages'
        uses: actions/cache@v4
        with:
          path: |
            .fallout/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-release-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props', 'version.json') }}
      - name: 'Run: Test, Pack, Publish'
        run: ./build.cmd Test Pack Publish
        env:
          # Publish target is nuget.org now that Fallout.* rename has landed (#54).
          # NUGET_API_KEY is scoped to push Fallout.* packages; it resolves from
          # the `nuget-org` GitHub Environment declared on this job (#273).
          NuGetApiKey: ${{ secrets.NUGET_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Nuke.* transition-shim packages are intentionally filtered out of the
      # main Publish step (see Build.cs PushPackageFiles) because that ID
      # belongs to NUKE's original maintainer on nuget.org. We publish them
      # here to GitHub Packages instead. #47.
      - name: 'Publish: Nuke.* transition shims to GH Packages'
        if: always()  # still push the shims even if a previous Fallout.* push transient-failed
        run: |
          shopt -s nullglob
          shims=(output/packages/Nuke.*.nupkg)
          if [ ${#shims[@]} -eq 0 ]; then
            echo "No Nuke.* shim packages found in output/packages — skipping."
            exit 0
          fi
          for pkg in "${shims[@]}"; do
            echo "Pushing $pkg to GH Packages..."
            dotnet nuget push "$pkg" \
              --source "https://nuget.pkg.github.com/Fallout-build/index.json" \
              --api-key "${{ secrets.GITHUB_TOKEN }}" \
              --skip-duplicate
          done
